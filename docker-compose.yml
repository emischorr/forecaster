version: "3.7"
services:
  forecaster:
    image: emischorr/forecaster:latest
    restart: unless-stopped
    environment:
      - FORECAST_PLACE=
      - MQTT_HOST=
      - MQTT_PORT=1883
      - MQTT_USER=
      - MQTT_PW=